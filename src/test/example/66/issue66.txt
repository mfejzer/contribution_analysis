Perhaps as an option that developers could turn-on in global.R? The diff is for R/shiny.R@@ -671,7 +671,7 @@ startApp <- function(port=8101L) {   serverFileTimestamp <- NULL   local({     # serverFileTimestamp <<- file.info(serverR)$mtime+    serverFileTimestamp <<- max(file.info(dir(recursive=TRUE))$mtime)   # mychange     source(serverR, local=new.env(parent=.GlobalEnv))     if (is.null(.globals$server))       stop("No server was defined in server.R")@@ -750,7 +750,7 @@ startApp <- function(port=8101L) {                  # Check if server.R has changed, and if so, reload         # mtime <- file.info(serverR)$mtime[1]+        mtime <- max(file.info(dir(recursive=TRUE))$mtime)    # mychange         if (!identical(mtime, serverFileTimestamp)) {           shinyServer(NULL)           local({
