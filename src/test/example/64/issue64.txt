https://groups.google.com/forum/?fromgroups=#!topic/shiny-discuss/0NPKZwrH6Sg```The reactivity sample creates two id=caption; something like    output$captionOut <- reactiveText(function() {      input$caption    })and     h3(textOutput("captionOut"))would be better. However, the latter produces illegal code (as all h3)    <h3>      <div id="captionOut" class="shiny-text-output"></div>    </h3>which is "Element div not allowed as child of element h3 in this case".```
